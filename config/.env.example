# Optional environment fallback if not using config file
SLACK_WEBHOOK=
DISCORD_WEBHOOK=
SMTP_SERVER=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

# Path to config.json (optional)
SIEM_CONFIG_PATH=config/config.json
